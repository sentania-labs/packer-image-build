
## 2026-04-23 06:18 — windows packer 1.11 pin

**Type:** status
**Needs Scott:** false

### What I did
- Relaxed `required_version` in `windows/windows.pkr.hcl` from `>= 1.11.2` to `>= 1.11.0, < 1.12.0` so the template runs on whatever 1.11.x the self-hosted runners have.

### What I learned
- Nothing else in the Windows template is Packer-1.12-specific (`reattach_cdroms`, `cd_content`, content_library_destination, vsphere plugin 1.4.2 pin are all fine on Packer 1.11.x).

### What's next or blocked
- Asked Scott whether the vsphere plugin pin should also be tightened to a 1.11-compatible range; awaiting answer. No CI change made.

## 2026-04-23 07:34 — fix-windows2025-iso-filename  [batch]

**Type:** status

Fixed `windows_iso_path` in `windows/variables.auto.pkrvars.hcl` — replaced placeholder `server2025-iso/file.iso` with the real ISO filename `26100.1742.240906-0331.ge_release_svc_refresh_SERVER_EVAL_x64FRE_en-us.iso` and updated the adjacent comment. Resolves the "Invalid datastore path" failure for both `windows2025-bare` and `windows2025-cbinit`. Committed `a452934` and pushed to `main`. `packer validate` not run — packer binary is not installed in this workspace.

## 2026-04-23 17:00 — win2025 vmxnet3 known-issue doc

**Type:** fyi
**Needs Scott:** false

### What I did
- Created `windows/NOTES.md` documenting the Win2025 vmxnet3 driver gap (install media lacks vmxnet3 → no NIC driver → DHCP never fires → Packer IP-wait hangs).
- Captured both fix approaches (slipstream into boot.wim/install.wim; or attach driver + autounattend DriverPaths entry) and referenced the older sentania/windowsServer_ImageBuild repo as a working example.
- Committed as `fd2142a` and pushed to main.

### What I learned
- Windows dir had per-variant docs (bare.md, cloudbase-init.md, README.md) but no cross-variant issues file — NOTES.md is now the idiomatic spot for build-wide Windows heads-ups.

### What's next or blocked
- Both windows2025-bare and windows2025-cbinit builds remain broken on this issue until driver injection is wired into the build. Not attempted this session.
